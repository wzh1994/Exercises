#include <stdio.h>
#include <iostream>
#include <cstdlib>
#include <map>
#include <iterator>
using namespace std;
void testSub();
void show();
map<int, double*> mp;
void test(){
	int a, b,c,d,e;
	char f;
	while (cin >> a ){
		cin >> b >> f>> c >> f >> d >>f>> e;
		cout << a << " " << b << " " << c << " " <<d << " " <<e << endl;
	}
	
}
